# Antigravity Chatbot
## Deploy Steps:
1. Replace `GOOGLE_API_KEY` in `.env` (local) and Render env vars.
2. Push to GitHub.
3. Deploy on Render as a Web Service.
4. Update `API_URL` in `index.html` with your Render URL.
